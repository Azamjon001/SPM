# 🚀 ПОЛНЫЙ ПРОМПТ ДЛЯ СОЗДАНИЯ ПРОЕКТА С НУЛЯ

**Версия:** 2.0 | **Дата:** 10.01.2026

---

## 📋 ИНСТРУКЦИЯ ДЛЯ AI

Создай полнофункциональное **enterprise-уровневое SPA приложение** для управления товарами компаний со следующими требованиями:

---

## 🎯 ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА

### Назначение
Платформа для управления товарами с тремя типами пользователей:
1. **Покупатели** (Customers) - покупают товары
2. **Компании** (Companies) - продают товары
3. **Администратор** (Admin) - управляет всей системой

### Ключевые особенности
- Мультиязычность (Русский + Узбекский)
- Система публичных и приватных компаний
- Real-time обновления данных
- 3-уровневая система оплаты
- Продвинутая аналитика
- QR/штрих-код сканирование
- Excel импорт/экспорт

---

## 🛠️ ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Frontend
```
⚛️  React 18 с TypeScript
🎨 Tailwind CSS v4
🔥 Supabase Client (для Realtime и Auth)
📊 @tanstack/react-query (кэширование)
🎨 shadcn/ui + Radix UI (UI компоненты)
📊 recharts (графики)
📄 xlsx (SheetJS) (Excel)
📱 html5-qrcode (QR/штрих-код сканер)
🔐 bcryptjs (хеширование паролей)
🍞 sonner (toast уведомления)
🎨 lucide-react (иконки)
🎠 react-slick (карусели)
```

### Backend
```
🔥 Supabase (BaaS)
   - PostgreSQL (база данных)
   - Realtime (WebSocket)
   - Auth (аутентификация)
   - Storage (хранилище файлов)
   - Edge Functions (Deno + Hono)
```

### База данных (PostgreSQL)
```sql
-- Таблицы:
- kv_store_6a74b22c (key-value хранилище)
- products (товары)
- companies (компании)
- users (пользователи)
- customer_orders (заказы)
- ads (реклама)
- payments (платежи)
- expenses (расходы)
```

---

## 🏗️ АРХИТЕКТУРА ПРИЛОЖЕНИЯ

### Структура файлов
```
/
├── App.tsx                          # Root компонент
├── components/
│   ├── LoginPage.tsx               # Вход для покупателей
│   ├── SmsVerification.tsx         # SMS верификация
│   ├── CompanyLogin.tsx            # Вход для компаний
│   ├── CompanyKeyVerification.tsx  # Верификация ключа
│   ├── HomePage.tsx                # Главная покупателя
│   ├── LikesPage.tsx               # Избранное
│   ├── SettingsPage.tsx            # Настройки
│   ├── CompanyPanel.tsx            # Панель компании
│   ├── AdminPanel.tsx              # Панель админа
│   ├── PaymentPage.tsx             # Страница оплаты
│   ├── DigitalWarehouse.tsx        # Цифровой склад
│   ├── AnalyticsPanel.tsx          # Аналитика
│   ├── ProductCard.tsx             # Карточка товара
│   ├── ImageUploader.tsx           # Загрузка изображений
│   ├── ExcelColumnMapper.tsx       # Маппинг Excel колонок
│   ├── BarcodeSearchPanel.tsx      # Поиск по штрих-коду
│   ├── CompanySMMPanel.tsx         # SMM панель
│   ├── ExpensesManager.tsx         # Управление расходами
│   ├── OrdersPanel.tsx             # Панель заказов
│   ├── BottomNavigation.tsx        # Нижняя навигация
│   ├── ApprovedAdsBanner.tsx       # Баннер рекламы
│   ├── CompanyManagement.tsx       # Управление компаниями
│   ├── AdminAdsPanel.tsx           # Модерация рекламы
│   ├── CustomerRegistrationModeSelector.tsx  # Выбор режима регистрации
│   ├── PrivateCompanyAccess.tsx    # Доступ к приватной компании
│   ├── CompanyModeSelector.tsx     # Выбор режима компании
│   ├── CompanyRegistrationForm.tsx # Регистрация компании
│   └── ui/                         # UI компоненты (40+)
├── utils/
│   ├── api.tsx                     # API клиент
│   ├── cache.tsx                   # React Query кэш
│   ├── realtimeCache.tsx           # Realtime + RAM кэш
│   ├── translations.tsx            # Локализация
│   ├── ThemeContext.tsx            # Темная/светлая тема
│   ├── navigation.tsx              # Навигация
│   ├── excelHelper.tsx             # Excel утилиты
│   ├── debounce.tsx                # Debounce функция
│   └── supabase/
│       ├── client.tsx              # Supabase клиент
│       └── info.tsx                # Конфигурация
└── styles/
    └── globals.css                 # Tailwind + кастомные стили
```

---

## 👤 СИСТЕМА ПОЛЬЗОВАТЕЛЕЙ

### 1. ПОКУПАТЕЛИ (Customers)

#### Регистрация и вход

**Шаг 1: Выбор режима регистрации**
- Показать компонент `CustomerRegistrationModeSelector`
- Две кнопки:
  - **"Публичная регистрация"** - доступ ко всем публичным компаниям
  - **"Приватная регистрация"** - доступ к одной приватной компании

**Если выбрана публичная:**
```
1. Перейти на LoginPage
2. Ввод номера телефона (формат: 998901234567)
3. Отправка SMS с 6-значным кодом
4. Переход на SmsVerification
5. Ввод кода (например: 123456)
6. Проверка кода в базе данных
7. Если верно → сохранить в localStorage:
   - customer_phone
   - customer_registration_mode: "public"
8. Перейти на HomePage
```

**Если выбрана приватная:**
```
1. Перейти на PrivateCompanyAccess
2. Показать поле ввода: "Введите ID компании"
3. Пользователь вводит company_id (например: "ABC123XYZ")
4. Проверить в базе данных:
   - SELECT * FROM companies WHERE company_id = 'ABC123XYZ' AND is_private = true
5. Если не найдено → показать ошибку "Компания не найдена"
6. Если найдено → перейти на LoginPage
7. Ввод номера телефона
8. Отправка SMS с кодом
9. Переход на SmsVerification
10. Ввод кода
11. Проверка кода
12. Если верно → сохранить в localStorage:
    - customer_phone
    - customer_registration_mode: "private"
    - private_company_id: "ABC123XYZ"
13. Перейти на HomePage
```

#### HomePage - Главная страница покупателя

**Структура страницы:**

```
┌─────────────────────────────────────────┐
│  HEADER                                 │
│  ┌─────────┐  [Поиск]  ☀️/🌙  RU/UZ   │
│  │  LOGO   │                            │
│  └─────────┘                            │
├─────────────────────────────────────────┤
│  APPROVED ADS BANNER (Карусель)         │
│  [← Реклама 1 →] [← Реклама 2 →]      │
├─────────────────────────────────────────┤
│  BARCODE SCANNER                        │
│  📱 [Сканировать QR/Штрих-код]         │
├─────────────────────────────────────────┤
│  CATEGORY FILTER                        │
│  [Все] [Электроника] [Одежда] [Еда]... │
├─────────────────────────────────────────┤
│  PRODUCTS GRID                          │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐  │
│  │Product 1│ │Product 2│ │Product 3│  │
│  │[Image]  │ │[Image]  │ │[Image]  │  │
│  │Название │ │Название │ │Название │  │
│  │Компания │ │Компания │ │Компания │  │
│  │1000₸💰  │ │2000₸💰  │ │3000₸💰  │  │
│  │❤️ 🛒 🎨  │ │❤️ 🛒 🎨  │ │❤️ 🛒 🎨  │  │
│  └─────────┘ └─────────┘ └─────────┘  │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐  │
│  │Product 4│ │Product 5│ │Product 6│  │
│  └─────────┘ └─────────┘ └─────────┘  │
├─────────────────────────────────────────┤
│  BOTTOM NAVIGATION                      │
│  [🏠 Магазин] [🛒 Корзина] [📦 Заказы] │
└─────────────────────────────────────────┘
```

**Логика загрузки товаров:**

1. При загрузке страницы:
```typescript
// Если публичная регистрация
if (customer_registration_mode === "public") {
  // Загрузить все товары от публичных компаний
  const products = await fetch('/products')
    .then(res => res.json())
  
  // Фильтровать только товары от публичных компаний
  const filteredProducts = products.filter(product => {
    const company = companies.find(c => c.id === product.company_id)
    return company && !company.is_private && product.available_to_customers
  })
}

// Если приватная регистрация
if (customer_registration_mode === "private") {
  const company_id = localStorage.getItem('private_company_id')
  
  // Загрузить товары только этой компании
  const products = await fetch(`/products?company_id=${company_id}`)
    .then(res => res.json())
  
  // Показать только товары этой компании
  const filteredProducts = products.filter(p => 
    p.company_id === company_id && p.available_to_customers
  )
}
```

**Карточка товара (ProductCard):**

Каждая карточка должна содержать:

```
┌──────────────────────────────┐
│  [📸 Изображение товара]      │  ← Клик открывает карусель
│  (или 🖼️ placeholder)         │     всех 5 изображений
├──────────────────────────────┤
│  Название товара              │  ← product.name
│  📍 Название компании          │  ← company.name
├──────────────────────────────┤
│  Цена: 1000₸                  │  ← Рассчитанная цена
│  (база: 800₸ + наценка 25%)  │     с наценкой
├──────────────────────────────┤
│  Остаток: 50 шт               │  ← product.quantity
├──────────────────────────────┤
│  🎨 Цвета: [🔴][🔵][🟢]        │  ← Выбор цвета (если есть)
├──────────────────────────────┤
│  ❤️ Лайк  |  🛒 В корзину     │  ← Действия
└──────────────────────────────┘
```

**Расчет цены с наценкой:**
```typescript
const finalPrice = product.price * (1 + product.markup_percent / 100)
// Пример: 800 * (1 + 25/100) = 800 * 1.25 = 1000₸
```

**Функционал карточки:**

1. **Клик на изображение:**
   - Открыть полноэкранную карусель с всеми изображениями товара (до 5)
   - Свайпы влево/вправо для переключения
   - Кнопка X для закрытия

2. **Кнопка ❤️ Лайк:**
   ```typescript
   const handleLike = (productId: number) => {
     // Если уже лайкнут - убрать лайк
     if (likedProductIds.includes(productId)) {
       const newLikes = likedProductIds.filter(id => id !== productId)
       setLikedProductIds(newLikes)
     } else {
       // Добавить лайк
       setLikedProductIds([...likedProductIds, productId])
     }
     
     // Сохранить в базу данных
     await saveUserLikes(customer_phone, likedProductIds)
     
     // Показать анимацию сердечка
     showLikeAnimation()
   }
   ```

3. **Кнопка 🛒 В корзину:**
   ```typescript
   const handleAddToCart = (productId: number) => {
     // Увеличить количество в корзине
     setCart({
       ...cart,
       [productId]: (cart[productId] || 0) + 1
     })
     
     // Показать toast уведомление
     toast.success('Добавлено в корзину!')
   }
   ```

4. **Выбор цвета (если product.colors существует):**
   ```typescript
   const handleColorChange = (productId: number, color: string) => {
     setSelectedColors({
       ...selectedColors,
       [productId]: color
     })
     
     // Сохранить в localStorage
     localStorage.setItem('selectedColors', JSON.stringify(selectedColors))
     
     // Добавить радужную анимацию карточке
     cardElement.classList.add('rainbow-card-animation-light')
   }
   ```

**QR/Штрих-код сканер:**

```
Кнопка: "📱 Сканировать штрих-код"

При клике:
1. Открыть камеру
2. Показать видео поток с наложенной рамкой сканирования
3. Сканировать QR код или штрих-код
4. При обнаружении кода:
   - Поиск в базе по product.barcode или product.barid
   - Если найдено → показать карточку товара
   - Если не найдено → показать "Товар не найден"
```

**Поиск:**
```typescript
const handleSearch = (query: string) => {
  // Поиск по названию, категории, штрих-коду
  const results = products.filter(product => 
    product.name.toLowerCase().includes(query.toLowerCase()) ||
    product.category.toLowerCase().includes(query.toLowerCase()) ||
    product.barcode?.includes(query) ||
    product.barid?.includes(query)
  )
  
  setFilteredProducts(results)
}
```

**Корзина (вкладка в BottomNavigation):**

```
┌──────────────────────────────────────┐
│  КОРЗИНА                             │
├──────────────────────────────────────┤
│  ┌────────────────────────────────┐  │
│  │ [📸] Товар 1                   │  │
│  │ Компания: ООО "Рога и копыта"  │  │
│  │ Цена: 1000₸                    │  │
│  │ Количество: [−] 2 [+]          │  │
│  │ Итого: 2000₸        [🗑️]       │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ [📸] Товар 2                   │  │
│  │ Компания: ООО "Рога и копыта"  │  │
│  │ Цена: 500₸                     │  │
│  │ Количество: [−] 1 [+]          │  │
│  │ Итого: 500₸         [🗑️]       │  │
│  └────────────────────────────────┘  │
├──────────────────────────────────────┤
│  ИТОГО: 2500₸                        │
│  [💳 Оформить заказ]                 │
└──────────────────────────────────────┘
```

**Оформление заказа:**
```typescript
const handleCheckout = async () => {
  // 1. Сгенерировать receipt_code
  const receiptCode = `REC-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`
  
  // 2. Создать заказ в базе данных
  const order = {
    receipt_code: receiptCode,
    customer_phone: customer_phone,
    company_id: cart[0].company_id, // Берем company_id первого товара
    items: Object.entries(cart).map(([productId, quantity]) => ({
      product_id: Number(productId),
      quantity: quantity,
      price: products.find(p => p.id === Number(productId)).price,
      final_price: calculatePriceWithMarkup(productId)
    })),
    total_amount: calculateTotal(cart),
    status: 'pending',
    payment_method: null,
    created_at: new Date().toISOString()
  }
  
  await fetch('/orders', {
    method: 'POST',
    body: JSON.stringify(order)
  })
  
  // 3. Перейти на PaymentPage
  navigate('payment', { receiptCode })
}
```

**PaymentPage - Страница оплаты:**

```
┌──────────────────────────────────────┐
│  ЧЕК №REC-1234567890-abc123         │
├──────────────────────────────────────┤
│  Товары:                             │
│  • Товар 1 x2 = 2000₸                │
│  • Товар 2 x1 = 500₸                 │
│                                      │
│  Итого: 2500₸                        │
├──────────────────────────────────────┤
│  Выберите способ оплаты:             │
│                                      │
│  [💳 Payme]                          │
│  [💳 Click]                          │
│  [💵 Наличные (при получении)]       │
└──────────────────────────────────────┘
```

**Логика оплаты:**

1. **Payme:**
```typescript
const payWithPayme = () => {
  // Redirect на Payme шлюз
  const params = {
    merchant_id: PAYME_MERCHANT_ID,
    amount: totalAmount * 100, // в тийинах
    account: {
      order_id: receiptCode
    },
    callback_url: `https://${projectId}.supabase.co/functions/v1/make-server-6a74b22c/payme-callback`
  }
  
  window.location.href = `https://checkout.paycom.uz/${base64encode(params)}`
}
```

2. **Click:**
```typescript
const payWithClick = () => {
  // Redirect на Click шлюз
  const params = {
    merchant_id: CLICK_MERCHANT_ID,
    service_id: CLICK_SERVICE_ID,
    amount: totalAmount,
    transaction_param: receiptCode,
    return_url: `https://${projectId}.supabase.co/functions/v1/make-server-6a74b22c/click-callback`
  }
  
  window.location.href = `https://my.click.uz/services/pay?${new URLSearchParams(params)}`
}
```

3. **После успешной оплаты:**
```typescript
// Callback от платежной системы на сервер
// Server обновляет статус заказа и списывает товары:

// 1. Обновить статус
UPDATE customer_orders 
SET status = 'paid', payment_method = 'payme' 
WHERE receipt_code = 'REC-...'

// 2. АВТОМАТИЧЕСКИ СПИСАТЬ ТОВАРЫ СО СКЛАДА
for (const item of order.items) {
  UPDATE products 
  SET quantity = quantity - item.quantity 
  WHERE id = item.product_id
}

// 3. Отправить уведомление покупателю
toast.success('Оплата прошла успешно! Заказ оформлен.')

// 4. Очистить корзину
setCart({})

// 5. Перейти на страницу "Мои заказы"
navigate('orders')
```

**Мои заказы (вкладка в BottomNavigation):**

```
┌──────────────────────────────────────┐
│  МОИ ЗАКАЗЫ                          │
├──────────────────────────────────────┤
│  ┌────────────────────────────────┐  │
│  │ Заказ #REC-1234567890-abc123   │  │
│  │ Дата: 10.01.2026 15:30         │  │
│  │ Компания: ООО "Рога и копыта"  │  │
│  │ Сумма: 2500₸                   │  │
│  │ Статус: ✅ Оплачено            │  │
│  │ [Подробнее]                    │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ Заказ #REC-9876543210-xyz789   │  │
│  │ Дата: 09.01.2026 12:15         │  │
│  │ Компания: ООО "Рога и копыта"  │  │
│  │ Сумма: 5000₸                   │  │
│  │ Статус: ⏳ Ожидает оплаты      │  │
│  │ [Подробнее] [💳 Оплатить]      │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
```

**Избранное (LikesPage):**
- Показать все товары которые пользователь лайкнул
- Такие же карточки как на HomePage
- Можно добавлять в корзину
- Можно убирать из избранного

---

### 2. КОМПАНИИ (Companies)

#### Регистрация и вход

**Регистрация новой компании (только через Админа):**
```
Админ создает компанию с полями:
- name: string (название компании)
- phone: string (телефон, формат: 998901234567)
- password: string (будет захеширован bcrypt)
- access_key: string (30-значный ключ, генерируется автоматически)
- is_private: boolean (публичная или приватная)
- company_id: string | null (если приватная - уникальный ID, например "ABC123XYZ")
- logo_url: string | null (URL логотипа)
- address: string | null (адрес)
- description: string | null (описание)
```

**Вход компании:**

```
1. Показать CompanyLogin:
   ┌──────────────────────────────┐
   │  ВХОД ДЛЯ КОМПАНИЙ           │
   ├──────────────────────────────┤
   │  Телефон: [_____________]    │
   │  Пароль:  [_____________]    │
   │  [Войти]                     │
   └──────────────────────────────┘

2. Пользователь вводит:
   - phone: "998909383572"
   - password: "24067"

3. Проверка на сервере:
   const company = await SELECT * FROM companies WHERE phone = '998909383572'
   const passwordMatch = await bcrypt.compare('24067', company.password_hash)
   
   if (!passwordMatch) {
     return error "Неверный пароль"
   }

4. Перейти на CompanyKeyVerification:
   ┌──────────────────────────────┐
   │  ВВЕДИТЕ КЛЮЧ ДОСТУПА        │
   ├──────────────────────────────┤
   │  Ключ (30 символов):         │
   │  [____________________________]│
   │  [Подтвердить]               │
   └──────────────────────────────┘

5. Пользователь вводит 30-значный ключ

6. Проверка:
   if (inputKey !== company.access_key) {
     return error "Неверный ключ доступа"
   }

7. Если верно:
   - Сохранить в localStorage:
     - company_id: company.id
     - company_phone: company.phone
     - company_name: company.name
   - Перейти на CompanyPanel
```

#### CompanyPanel - Панель компании

**Структура (с вкладками):**

```
┌────────────────────────────────────────────────────┐
│  🏢 Компания: ООО "Рога и копыта"                  │
│  [Выход] [⚙️ Настройки] [☀️/🌙] [RU/UZ]           │
├────────────────────────────────────────────────────┤
│  TABS:                                             │
│  [📦 Склад] [📊 Аналитика] [📋 Заказы]            │
│  [🎨 Реклама] [💳 Платежи] [💸 Расходы]            │
│  [👤 Профиль] [💬 Чат] [💰 Продажи]                │
├────────────────────────────────────────────────────┤
│                                                    │
│  СОДЕРЖИМОЕ ТЕКУЩЕЙ ВКЛАДКИ                        │
│                                                    │
└────────────────────────────────────────────────────┘
```

**Вкладка "📦 Склад" (DigitalWarehouse):**

```
┌────────────────────────────────────────┐
│  ЦИФРОВОЙ СКЛАД                        │
├────────────────────────────────────────┤
│  [➕ Добавить товар]                   │
│  [📊 Импорт Excel] [📤 Экспорт Excel]  │
│  [🏷️ Управление категориями]          │
├────────────────────────────────────────┤
│  Поиск: [_________________] 🔍         │
│  Категория: [Все ▾] Сортировка: [▾]   │
├────────────────────────────────────────┤
│  ТАБЛИЦА ТОВАРОВ:                      │
│  ┌──┬──────────┬────┬─────┬──────┐   │
│  │#│ Название │Кол │Цена │Действия│  │
│  ├──┼──────────┼────┼─────┼──────┤   │
│  │1│ Товар 1  │ 50 │1000₸│✏️ 🗑️ │   │
│  │2│ Товар 2  │ 30 │2000₸│✏️ 🗑️ │   │
│  │3│ Товар 3  │ 10 │3000₸│✏️ 🗑️ │   │
│  └──┴──────────┴────┴─────┴──────┘   │
└────────────────────────────────────────┘
```

**Добавление товара:**

Форма с полями:
```
┌──────────────────────────────────────┐
│  ДОБАВИТЬ ТОВАР                      │
├──────────────────────────────────────┤
│  Название*: [___________________]    │
│  Количество*: [___________________]  │
│  Базовая цена*: [___________________]│
│  Наценка (%)*: [___________________] │
│  → Итоговая цена: 1250₸             │
│                                      │
│  Штрих-код: [___________________]    │
│  Barid: [___________________]        │
│  Категория*: [Выбрать ▾]            │
│                                      │
│  Изображения (до 5):                 │
│  [📤 Загрузить] [📸 Превью]         │
│                                      │
│  Цвета (опционально):                │
│  [+ Добавить цвет]                   │
│  🔴 Красный [x]                      │
│  🔵 Синий [x]                        │
│                                      │
│  Видно покупателям: [✓]              │
│                                      │
│  [Отмена] [Сохранить]                │
└──────────────────────────────────────┘
```

**Логика сохранения:**
```typescript
const handleSaveProduct = async () => {
  // Валидация
  if (!name || !quantity || !price || !markup_percent || !category) {
    toast.error('Заполните все обязательные поля')
    return
  }
  
  // Расчет итоговой цены
  const finalPrice = price * (1 + markup_percent / 100)
  
  // Загрузка изображений в Supabase Storage
  const uploadedImages = []
  for (const image of images) {
    const { data } = await supabase.storage
      .from('product-images')
      .upload(`${company_id}/${Date.now()}_${image.name}`, image)
    
    const { data: { publicUrl } } = supabase.storage
      .from('product-images')
      .getPublicUrl(data.path)
    
    uploadedImages.push(publicUrl)
  }
  
  // Создание товара
  const product = {
    name,
    quantity: Number(quantity),
    price: Number(price),
    markup_percent: Number(markup_percent),
    barcode: barcode || null,
    barid: barid || null,
    category,
    company_id: company_id, // ВАЖНО! Всегда должен быть указан
    images: uploadedImages,
    colors: colors,
    available_to_customers: availableToCustomers,
    created_at: new Date().toISOString()
  }
  
  await fetch('/products', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(product)
  })
  
  toast.success('Товар добавлен!')
  
  // Обновить список товаров
  refetchProducts()
}
```

**ВАЖНО: Система категорий с маркерами**

```typescript
// При создании пустой категории (без товаров):
const createEmptyCategory = async (categoryName: string) => {
  // Создаем товар-маркер
  const marker = {
    name: `__CATEGORY_MARKER__${categoryName}`,
    quantity: 0,
    price: 0,
    markup_percent: 0,
    category: categoryName,
    company_id: company_id,
    available_to_customers: false // Не видно покупателям
  }
  
  await fetch('/products', {
    method: 'POST',
    body: JSON.stringify(marker)
  })
}

// При добавлении первого реального товара в категорию:
const addFirstProductToCategory = async (product) => {
  // Удалить маркер если есть
  const marker = products.find(p => 
    p.name === `__CATEGORY_MARKER__${product.category}` &&
    p.company_id === company_id
  )
  
  if (marker) {
    await fetch(`/products/${marker.id}`, { method: 'DELETE' })
  }
  
  // Добавить товар
  await fetch('/products', {
    method: 'POST',
    body: JSON.stringify(product)
  })
}

// При удалении последнего товара в категории:
const deleteLastProductInCategory = async (productId, category) => {
  const remainingProducts = products.filter(p => 
    p.category === category &&
    p.company_id === company_id &&
    p.id !== productId &&
    !p.name.startsWith('__CATEGORY_MARKER__')
  )
  
  if (remainingProducts.length === 0) {
    // Создать маркер
    await createEmptyCategory(category)
  }
  
  // Удалить товар
  await fetch(`/products/${productId}`, { method: 'DELETE' })
}

// При отображении товаров - фильтровать маркеры:
const displayProducts = products.filter(p => 
  !p.name || !p.name.startsWith('__CATEGORY_MARKER__')
)
```

**Редактирование товара:**
```typescript
const handleEditProduct = (productId: number) => {
  // Загрузить данные товара
  const product = products.find(p => p.id === productId)
  
  // Заполнить форму
  setEditForm(product)
  
  // Показать модальное окно с формой (как при добавлении)
  setIsEditModalOpen(true)
}

const handleUpdateProduct = async () => {
  // ВАЖНО: Всегда включать company_id при обновлении!
  const updates = {
    name: editForm.name,
    quantity: Number(editForm.quantity),
    price: Number(editForm.price),
    markup_percent: Number(editForm.markup_percent),
    barcode: editForm.barcode,
    barid: editForm.barid,
    category: editForm.category,
    company_id: company_id, // НЕ ЗАБЫТЬ!!!
    images: editForm.images,
    colors: editForm.colors,
    available_to_customers: editForm.available_to_customers
  }
  
  await fetch(`/products/${productId}`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(updates)
  })
  
  toast.success('Товар обновлен!')
  refetchProducts()
}
```

**Удаление товара:**
```typescript
const handleDeleteProduct = async (productId: number) => {
  // Подтверждение
  if (!confirm('Вы уверены что хотите удалить этот товар?')) {
    return
  }
  
  // Получить категорию товара
  const product = products.find(p => p.id === productId)
  
  // Проверить - это последний товар в категории?
  const categoryProducts = products.filter(p => 
    p.category === product.category &&
    p.company_id === company_id &&
    !p.name.startsWith('__CATEGORY_MARKER__')
  )
  
  // Удалить товар
  await fetch(`/products/${productId}`, {
    method: 'DELETE'
  })
  
  // Если это был последний товар - создать маркер
  if (categoryProducts.length === 1) {
    await createEmptyCategory(product.category)
  }
  
  toast.success('Товар удален!')
  refetchProducts()
}
```

**Импорт из Excel:**

```
1. Кнопка "📊 Импорт Excel"

2. Открыть модальное окно:
   ┌──────────────────────────────────┐
   │  ИМПОРТ ИЗ EXCEL                 │
   ├──────────────────────────────────┤
   │  [📁 Выбрать файл]               │
   │  Или перетащите сюда             │
   └──────────────────────────────────┘

3. После выбора файла - показать ExcelColumnMapper:
   ┌──────────────────────────────────┐
   │  НАСТРОЙКА ИМПОРТА               │
   ├──────────────────────────────────┤
   │  Колонки из Excel:               │
   │  A | B | C | D | E               │
   │  Товар | Цена | Кол-во | ...    │
   │                                  │
   │  Сопоставьте с полями:           │
   │  A → [Название ▾]                │
   │  B → [Базовая цена ▾]            │
   │  C → [Количество ▾]              │
   │  D → [Наценка % ▾]               │
   │  E → [Категория ▾]               │
   │                                  │
   │  Предпросмотр (первые 5 строк): │
   │  ┌────────────────────────────┐  │
   │  │Товар 1 | 800 | 50 | 25 |..│  │
   │  │Товар 2 | 600 | 30 | 20 |..│  │
   │  └────────────────────────────┘  │
   │                                  │
   │  [Отмена] [Импортировать]        │
   └──────────────────────────────────┘

4. При клике "Импортировать":
   - Прочитать все строки из Excel
   - Применить маппинг колонок
   - Валидировать данные
   - Массово создать товары через API
   - Показать результат: "Импортировано 50 товаров"
```

**Логика импорта:**
```typescript
const handleImportExcel = async (file: File, mapping: ColumnMapping) => {
  // Прочитать Excel
  const workbook = XLSX.read(await file.arrayBuffer())
  const worksheet = workbook.Sheets[workbook.SheetNames[0]]
  const data = XLSX.utils.sheet_to_json(worksheet)
  
  // Применить маппинг
  const products = data.map(row => ({
    name: row[mapping.name],
    quantity: Number(row[mapping.quantity]),
    price: Number(row[mapping.price]),
    markup_percent: Number(row[mapping.markup_percent]),
    category: row[mapping.category] || 'Без категории',
    barcode: row[mapping.barcode] || null,
    barid: row[mapping.barid] || null,
    company_id: company_id, // ВАЖНО!
    available_to_customers: true,
    images: [],
    colors: []
  }))
  
  // Валидация
  const validProducts = products.filter(p => 
    p.name && p.quantity > 0 && p.price > 0
  )
  
  if (validProducts.length === 0) {
    toast.error('Нет валидных товаров для импорта')
    return
  }
  
  // Массовый импорт
  await fetch('/products/bulk', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ products: validProducts })
  })
  
  toast.success(`Импортировано ${validProducts.length} товаров`)
  refetchProducts()
}
```

**Экспорт в Excel:**
```typescript
const handleExportExcel = () => {
  // Подготовить данные
  const data = products.map(p => ({
    'Название': p.name,
    'Количество': p.quantity,
    'Базовая цена': p.price,
    'Наценка %': p.markup_percent,
    'Итоговая цена': p.price * (1 + p.markup_percent / 100),
    'Категория': p.category,
    'Штрих-код': p.barcode || '',
    'Barid': p.barid || '',
    'Видно покупателям': p.available_to_customers ? 'Да' : 'Нет'
  }))
  
  // Создать worksheet
  const worksheet = XLSX.utils.json_to_sheet(data)
  
  // Создать workbook
  const workbook = XLSX.utils.book_new()
  XLSX.utils.book_append_sheet(workbook, worksheet, 'Товары')
  
  // Скачать
  XLSX.writeFile(workbook, `products_${Date.now()}.xlsx`)
  
  toast.success('Excel файл скачан')
}
```

**Управление категориями:**
```
Модальное окно:
┌──────────────────────────────────┐
│  КАТЕГОРИИ                       │
├──────────────────────────────────┤
│  [+ Создать категорию]           │
│                                  │
│  Список категорий:               │
│  • Электроника (15 товаров) ✏️ 🗑️│
│  • Одежда (8 товаров) ✏️ 🗑️      │
│  • Еда (0 товаров) ✏️ 🗑️         │
│  • Без категории (3 товара)     │
└──────────────────────────────────┘

Функции:
- Создать новую категорию (создается маркер)
- Переименовать категорию
- Удалить категорию (если есть товары - предупреждение)
```

**Вкладка "📊 Аналитика" (AnalyticsPanel):**

```
┌────────────────────────────────────────┐
│  АНАЛИТИКА ПРОДАЖ                      │
├────────────────────────────────────────┤
│  ФИЛЬТРЫ:                              │
│  [Сегодня] [Вчера] [Неделя] [Месяц]   │
│  [Год] [Свой период: ОТ [__] ДО [__]] │
│                                        │
│  Категория: [Все ▾]                    │
│  Цена: [___] - [___]                   │
├────────────────────────────────────────┤
│  КЛЮЧЕВЫЕ МЕТРИКИ:                     │
│  ┌──────────┐ ┌──────────┐ ┌────────┐│
│  │💰 Выручка│ │💸 Прибыль│ │📊 Сред.││
│  │  50000₸  │ │  12500₸  │ │ чек   ││
│  │          │ │          │ │ 2500₸ ││
│  └──────────┘ └──────────┘ └────────┘│
│  ┌──────────┐ ┌──────────┐ ┌────────┐│
│  │🔢 Заказов│ │📦 Товаров│ │👥 Покуп│
│  │    20    │ │  продано │ │ателей ││
│  │          │ │    45    │ │   15  ││
│  └──────────┘ └──────────┘ └────────┘│
├────────────────────────────────────────┤
│  ГРАФИКИ:                              │
│  [Выручка по дням] [🔍 Zoom]           │
│  ┌────────────────────────────────┐   │
│  │     Line Chart (recharts)      │   │
│  │  ↑                             │   │
│  │50k│      •──•                  │   │
│  │   │    •      •──•             │   │
│  │25k│  •              •          │   │
│  │  └─────────────────────────→   │   │
│  │   1  5  10  15  20  25  30    │   │
│  └────────────────────────────────┘   │
│                                        │
│  [Продажи по категориям]               │
│  ┌────────────────────────────────┐   │
│  │      Bar Chart (recharts)      │   │
│  │  ↑                             │   │
│  │50│ ███                         │   │
│  │  │ ███ ██                      │   │
│  │25│ ███ ██ █                    │   │
│  │  └──────────────────────→      │   │
│  │   Эл. Одж Еда                 │   │
│  └────────────────────────────────┘   │
├────────────────────────────────────────┤
│  ТОП ТОВАРЫ:                           │
│  1. [📸] Товар А - 15 продаж - 15000₸ │
│  2. [📸] Товар Б - 10 продаж - 10000₸ │
│  3. [📸] Товар В - 8 продаж - 8000₸   │
├────────────────────────────────────────┤
│  ИНСАЙТЫ:                              │
│  💡 Продажи выросли на 25% по сравнению│
│     с прошлой неделей                  │
│  ⚠️ Низкий остаток у товара "Товар А"  │
│     (осталось 5 шт)                    │
│  🔝 Лучший день продаж: 15.01.2026     │
│     (8500₸)                            │
└────────────────────────────────────────┘
```

**Логика расчета метрик:**
```typescript
const calculateMetrics = (orders: Order[], period: DateRange) => {
  // Фильтровать заказы по пе��иоду и company_id
  const filteredOrders = orders.filter(order => 
    order.company_id === company_id &&
    order.status === 'paid' &&
    isWithinPeriod(order.created_at, period)
  )
  
  // Выручка
  const totalRevenue = filteredOrders.reduce((sum, order) => 
    sum + order.total_amount, 0
  )
  
  // Прибыль (считаем наценку)
  const totalProfit = filteredOrders.reduce((sum, order) => {
    const profit = order.items.reduce((itemSum, item) => {
      const product = products.find(p => p.id === item.product_id)
      const basePrice = product.price * item.quantity
      const markup = basePrice * (product.markup_percent / 100)
      return itemSum + markup
    }, 0)
    return sum + profit
  }, 0)
  
  // Средний чек
  const averageCheck = totalRevenue / filteredOrders.length || 0
  
  // Количество заказов
  const orderCount = filteredOrders.length
  
  // Товаров продано
  const productsSold = filteredOrders.reduce((sum, order) => 
    sum + order.items.reduce((itemSum, item) => itemSum + item.quantity, 0)
  , 0)
  
  // Уникальных покупателей
  const uniqueCustomers = new Set(
    filteredOrders.map(order => order.customer_phone)
  ).size
  
  return {
    totalRevenue,
    totalProfit,
    averageCheck,
    orderCount,
    productsSold,
    uniqueCustomers
  }
}
```

**Zoom функциональность для графиков:**
```typescript
const handleZoomChart = (chartType: string) => {
  // Открыть модальное окно во весь экран
  setZoomedChart(chartType)
  setIsZoomModalOpen(true)
  
  // Показать график в большем размере с интерактивными фичами:
  // - Tooltip с подробной информацией при наведении
  // - Возможность выделить область для зума
  // - Легенда
  // - Скачать график как изображение
}
```

**Вкладка "📋 Заказы" (OrdersPanel):**

```
┌────────────────────────────────────────┐
│  ЗАКАЗЫ                                │
├────────────────────────────────────────┤
│  Фильтр: [Все] [Ожидают] [Оплачены]   │
│  Поиск: [_________________] 🔍         │
├────────────────────────────────────────┤
│  ┌────────────────────────────────┐   │
│  │ #REC-1234567890-abc123         │   │
│  │ Покупатель: +998901234567      │   │
│  │ Дата: 10.01.2026 15:30         │   │
│  │ Товары: 3 шт                   │   │
│  │ Сумма: 2500₸                   │   │
│  │ Статус: ✅ Оплачено            │   │
│  │ [Подробнее]                    │   │
│  └────────────────────────────────┘   │
│  ┌────────────────────────────────┐   │
│  │ #REC-9876543210-xyz789         │   │
│  │ Покупатель: +998907654321      │   │
│  │ Дата: 09.01.2026 12:15         │   │
│  │ Товары: 1 шт                   │   │
│  │ Сумма: 5000₸                   │   │
│  │ Статус: ⏳ Ожидает оплаты      │   │
│  │ [Подробнее] [Отменить]         │   │
│  └────────────────────────────────┘   │
└────────────────────────────────────────┘
```

**Подробности заказа:**
```
Модальное окно:
┌────────────────────────────────────────┐
│  ЗАКАЗ #REC-1234567890-abc123          │
├────────────────────────────────────────┤
│  Покупатель: +998901234567             │
│  Дата: 10.01.2026 15:30:45             │
│  Способ оплаты: Payme                  │
│  Статус: ✅ Оплачено                   │
├────────────────────────────────────────┤
│  ТОВАРЫ:                               │
│  1. [📸] Товар 1                       │
│     Цена: 1000₸ x 2 = 2000₸            │
│  2. [📸] Товар 2                       │
│     Цена: 500₸ x 1 = 500₸              │
├────────────────────────────────────────┤
│  ИТОГО: 2500₸                          │
│  Из них прибыль: 625₸                  │
└────────────────────────────────────────┘
```

**Вкладка "🎨 Реклама" (CompanySMMPanel):**

```
┌────────────────────────────────────────┐
│  SMM ПАНЕЛЬ                            │
├────────────────────────────────────────┤
│  [+ Создать рекламу]                   │
├────────────────────────────────────────┤
│  МОИ ОБЪЯВЛЕНИЯ:                       │
│  ┌────────────────────────────────┐   │
│  │ [📸]                           │   │
│  │ Заголовок рекламы              │   │
│  │ Описание...                    │   │
│  │ Статус: ⏳ На модерации        │   │
│  │ Просмотров: 0                  │   │
│  │ [Редактировать] [Удалить]      │   │
│  └────────────────────────────────┘   │
│  ┌────────────────────────────────┐   │
│  │ [📸]                           │   │
│  │ Другая реклама                 │   │
│  │ Описание...                    │   │
│  │ Статус: ✅ Одобрено            │   │
│  │ Просмотров: 145                │   │
│  │ Действует до: 20.01.2026       │   │
│  │ [Редактировать] [Удалить]      │   │
│  └────────────────────────────────┘   │
└────────────────────────────────────────┘
```

**Создание рекламы:**
```
Модальное окно:
┌────────────────────────────────────────┐
│  СОЗДАТЬ РЕКЛАМУ                       │
├────────────────────────────────────────┤
│  Заголовок*: [___________________]     │
│  Описание*:                            │
│  [____________________________________]│
│  [____________________________________]│
│                                        │
│  Изображение*:                         │
│  [📤 Загрузить] или перетащите сюда   │
│  [📸 Превью]                           │
│                                        │
│  Целевая ссылка (опционально):         │
│  [___________________________________] │
│                                        │
│  Период показа:                        │
│  От: [10.01.2026] До: [20.01.2026]     │
│                                        │
│  Стоимость: 5000₸                      │
│                                        │
│  [Отмена] [Отправить на модерацию]     │
└────────────────────────────────────────┘
```

**Логика создания:**
```typescript
const handleCreateAd = async () => {
  // Загрузить изображение
  const { data } = await supabase.storage
    .from('ad-images')
    .upload(`${company_id}/${Date.now()}_${image.name}`, image)
  
  const { data: { publicUrl } } = supabase.storage
    .from('ad-images')
    .getPublicUrl(data.path)
  
  // Создать рекламу
  const ad = {
    company_id: company_id,
    title: title,
    description: description,
    image_url: publicUrl,
    target_url: targetUrl || null,
    status: 'pending', // На модерации
    views_count: 0,
    starts_at: startsAt,
    expires_at: expiresAt,
    created_at: new Date().toISOString()
  }
  
  await fetch('/ads', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(ad)
  })
  
  toast.success('Реклама отправлена на модерацию')
}
```

**Вкладка "💳 Платежи" (PaymentHistoryForCompany):**

```
┌────────────────────────────────────────┐
│  ИСТОРИЯ ПЛАТЕЖЕЙ                      │
├────────────────────────────────────────┤
│  Период: [Весь период ▾]               │
├────────────────────────────────────────┤
│  ┌────────────────────────────────┐   │
│  │ Дата: 10.01.2026               │   │
│  │ Сумма: 5000₸                   │   │
│  │ Тип: Комиссия админу (10%)     │   │
│  │ Статус: ✅ Оплачено            │   │
│  └────────────────────────────────┘   │
│  ┌────────────────────────────────┐   │
│  │ Дата: 03.01.2026               │   │
│  │ Сумма: 50000₸                  │   │
│  │ Тип: Продажи товаров           │   │
│  │ Статус: ✅ Получено            │   │
│  └────────────────────────────────┘   │
└────────────────────────────────────────┘
```

**Вкладка "💸 Расходы" (ExpensesManager):**

```
┌────────────────────────────────────────┐
│  УЧЕТ РАСХОДОВ                         │
├────────────────────────────────────────┤
│  [+ Добавить расход]                   │
├────────────────────────────────────────┤
│  СПИСОК РАСХОДОВ:                      │
│  ┌────────────────────────────────┐   │
│  │ Дата: 10.01.2026               │   │
│  │ Категория: Аренда              │   │
│  │ Сумма: 100000₸                 │   │
│  │ Описание: Аренда помещения     │   │
│  │ [Редактировать] [Удалить]      │   │
│  └────────────────────────────────┘   │
│  ┌────────────────────────────────┐   │
│  │ Дата: 08.01.2026               │   │
│  │ Категория: Зарплата            │   │
│  │ Сумма: 50000₸                  │   │
│  │ Описание: Зарплата сотруднику  │   │
│  │ [Редактировать] [Удалить]      │   │
│  └────────────────────────────────┘   │
├────────────────────────────────────────┤
│  ИТОГО ЗА МЕСЯЦ: 150000₸               │
└────────────────────────────────────────┘
```

**Вкладка "👤 Профиль" (CompanyProfile):**

```
┌────────────────────────────────────────┐
│  ПРОФИЛЬ КОМПАНИИ                      │
├────────────────────────────────────────┤
│  Логотип:                              │
│  [📸 Изображение] [Изменить]           │
│                                        │
│  Название*: [___________________]      │
│  Телефон*: [___________________]       │
│  Адрес: [___________________]          │
│  Описание:                             │
│  [____________________________________]│
│  [____________________________________]│
│                                        │
│  Режим: [• Публичная] [ Приватная]     │
│                                        │
│  Ключ доступа (30 символов):           │
│  [****************************] [👁️]  │
│                                        │
│  Пароль: [Изменить пароль]             │
│                                        │
│  [Сохранить изменения]                 │
└────────────────────────────────────────┘
```

---

### 3. АДМИНИСТРАТОР (Admin)

#### Вход администратора

```
СПЕЦИАЛЬНЫЙ ХАРДКОД:
Телефон: 914751330
Код: 15051

1. Скрытый путь (не показывается в UI)
2. Прямой вход без SMS
3. Постоянная сессия
```

#### AdminPanel - Панель администратора

**Структура:**

```
┌────────────────────────────────────────────────────┐
│  👑 ПАНЕЛЬ АДМИНИСТРАТОРА                          │
│  [Выход] [⚙️] [☀️/🌙] [RU/UZ]                     │
├────────────────────────────────────────────────────┤
│  TABS:                                             │
│  [🏢 Компании] [📊 Аналитика] [🎨 Реклама]        │
│  [💳 Платежи] [👥 Пользователи] [💬 Чат]          │
├────────────────────────────────────────────────────┤
│                                                    │
│  СОДЕРЖИМОЕ ТЕКУЩЕЙ ВКЛАДКИ                        │
│                                                    │
└────────────────────────────────────────────────────┘
```

**Вкладка "🏢 Компании" (CompanyManagement):**

```
┌────────────────────────────────────────┐
│  УПРАВЛЕНИЕ КОМПАНИЯМИ                 │
├────────────────────────────────────────┤
│  [+ Зарегистрировать новую компанию]   │
│  [🔄 Принудительная перезагрузка всех] │
├────────────────────────────────────────┤
│  Поиск: [_________________] 🔍         │
│  Фильтр: [Все] [Публичные] [Приватные]│
├────────────────────────────────────────┤
│  ТАБЛИЦА КОМПАНИЙ:                     │
│  ┌──┬──────────┬──────┬──────┬──────┐│
│  │#│ Название │Тел.  │Режим │Действ│││
│  ├──┼──────────┼──────┼──────┼──────┤│
│  │1│ООО "РиК" │998..│Публ. │✏️🗑️ │││
│  │2│ТОО "ABC" │998..│Прив. │✏️🗑️ │││
│  │3│ИП "XYZ"  │998..│Публ. │✏️🗑️ │││
│  └──┴──────────┴──────┴──────┴──────┘│
└────────────────────────────────────────┘
```

**Регистрация новой компании:**

```
Модальное окно:
┌────────────────────────────────────────┐
│  РЕГИСТРАЦИЯ КОМПАНИИ                  │
├────────────────────────────────────────┤
│  Название*: [___________________]      │
│  Телефон*: [___________________]       │
│  Пароль*: [___________________]        │
│                                        │
│  Режим компании:                       │
│  [• Публичная] [ Приватная]            │
│                                        │
│  Если приватная - ID компании:         │
│  [___________________]                 │
│  (Уникальный код, например ABC123XYZ)  │
│                                        │
│  Адрес: [___________________]          │
│  Описание:                             │
│  [____________________________________]│
│                                        │
│  Ключ доступа будет сгенерирован       │
│  автоматически (30 символов)           │
│                                        │
│  [Отмена] [Зарегистрировать]           │
└────────────────────────────────────────┘
```

**Логика регистрации:**
```typescript
const handleRegisterCompany = async () => {
  // Сгенерировать 30-значный ключ
  const accessKey = generateRandomString(30)
  // Пример: "aB3dE5gH7jK9mN2pQ4rS6tU8vW0xY1zA"
  
  // Хешировать пароль
  const passwordHash = await bcrypt.hash(password, 10)
  
  // Создать компанию
  const company = {
    name: name,
    phone: phone,
    password_hash: passwordHash,
    access_key: accessKey,
    is_private: isPrivate,
    company_id: isPrivate ? companyId : null,
    address: address || null,
    description: description || null,
    logo_url: null,
    created_at: new Date().toISOString()
  }
  
  await fetch('/companies', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(company)
  })
  
  // Показать сгенерированные данные
  alert(`
    Компания зарегистрирована!
    
    Телефон: ${phone}
    Пароль: ${password}
    Ключ доступа: ${accessKey}
    
    ВАЖНО: Сохраните эти данные!
    Ключ доступа показывается только один раз!
  `)
  
  toast.success('Компания зарегистрирована!')
  refetchCompanies()
}
```

**Принудительная перезагрузка всех устройств:**
```typescript
const handleForceReload = async () => {
  if (!confirm('Перезагрузить все устройства всех пользователей?')) {
    return
  }
  
  // Отправить команду на сервер
  await fetch('/force-reload', {
    method: 'POST'
  })
  
  // Broadcast через BroadcastChannel
  const channel = new BroadcastChannel('app-reload-channel')
  channel.postMessage({ type: 'FORCE_RELOAD' })
  
  toast.success('Команда перезагрузки отправлена всем устройствам')
}
```

**Вкладка "🎨 Реклама" (AdminAdsPanel):**

```
┌────────────────────────────────────────┐
│  МОДЕРАЦИЯ РЕКЛАМЫ                     │
├────────────────────────────────────────┤
│  Фильтр: [На модерации] [Одобренные]  │
│          [Отклоненные] [Истекшие]      │
├────────────────────────────────────────┤
│  ОБЪЯВЛЕНИЯ НА МОДЕРАЦИИ:              │
│  ┌────────────────────────────────┐   │
│  │ [📸 Изображение]               │   │
│  │ Заголовок рекламы              │   │
│  │ Описание объявления...         │   │
│  │                                │   │
│  │ Компания: ООО "Рога и копыта"  │   │
│  │ Период: 10.01 - 20.01.2026     │   │
│  │ Статус: ⏳ На модерации        │   │
│  │                                │   │
│  │ [✅ Одобрить] [❌ Отклонить]   │   │
│  └────────────────────────────────┘   │
└────────────────────────────────────────┘
```

**Логика модерации:**
```typescript
const handleApproveAd = async (adId: number) => {
  await fetch(`/ads/${adId}/status`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ status: 'approved' })
  })
  
  toast.success('Реклама одобрена!')
  refetchAds()
}

const handleRejectAd = async (adId: number) => {
  const reason = prompt('Причина отклонения:')
  
  await fetch(`/ads/${adId}/status`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ 
      status: 'rejected',
      rejection_reason: reason
    })
  })
  
  toast.success('Реклама отклонена')
  refetchAds()
}
```

**Вкладка "📊 Аналитика":**
```
Глобальная аналитика по всей системе:
- Общее количество компаний
- Общее количество пользователей
- Общий объем продаж
- Топ компании по продажам
- Топ товары по всей платформе
- Графики активности
```

**Вкладка "💳 Платежи":**
```
История всех платежей:
- От покупателей к компаниям
- От компаний к админу (комиссии)
- Фильтры по дате, компании, сумме
```

**Вкладка "👥 Пользователи":**
```
Список всех покупателей:
- Телефон
- Режим регистрации (публичный/приватный)
- Дата регистрации
- Количество заказов
- Общая сумма покупок
```

---

## 💾 СИСТЕМА КЭШИРОВАНИЯ (3 уровня)

### Уровень 1: RAM Cache (In-Memory)

```typescript
// utils/realtimeCache.tsx

class RAMCache {
  private cache = new Map<string, {
    data: any
    timestamp: number
    ttl: number
  }>()
  
  get<T>(key: string): T | null {
    const entry = this.cache.get(key)
    if (!entry) return null
    
    // Проверка TTL
    if (Date.now() - entry.timestamp > entry.ttl) {
      this.cache.delete(key)
      return null
    }
    
    console.log(`⚡ [RAM CACHE HIT] ${key}`)
    return entry.data as T
  }
  
  set<T>(key: string, data: T, ttl: number = 5 * 60 * 1000) {
    this.cache.set(key, {
      data,
      timestamp: Date.now(),
      ttl
    })
    console.log(`💾 [RAM CACHE SET] ${key} (TTL: ${ttl}ms)`)
  }
  
  delete(key: string) {
    this.cache.delete(key)
  }
  
  clear() {
    this.cache.clear()
  }
  
  // Автоочистка истекших записей
  cleanup() {
    const now = Date.now()
    for (const [key, entry] of this.cache.entries()) {
      if (now - entry.timestamp > entry.ttl) {
        this.cache.delete(key)
      }
    }
  }
}

export const ramCache = new RAMCache()

// Автоочистка каждые 2 минуты
setInterval(() => {
  ramCache.cleanup()
}, 2 * 60 * 1000)
```

### Уровень 2: localStorage Cache

```typescript
// utils/localStorageCache.tsx

class LocalStorageCache {
  private prefix = 'v2_'
  
  get<T>(key: string): T | null {
    try {
      const item = localStorage.getItem(this.prefix + key)
      if (!item) return null
      
      const { data, timestamp } = JSON.parse(item)
      
      // Проверка TTL (10 минут)
      if (Date.now() - timestamp > 10 * 60 * 1000) {
        this.remove(key)
        return null
      }
      
      console.log(`💾 [localStorage HIT] ${key}`)
      return data as T
    } catch (error) {
      console.error('localStorage error:', error)
      return null
    }
  }
  
  set<T>(key: string, data: T) {
    try {
      localStorage.setItem(
        this.prefix + key,
        JSON.stringify({
          data,
          timestamp: Date.now()
        })
      )
    } catch (error) {
      // QuotaExceededError - очищаем старые данные
      if (error.name === 'QuotaExceededError') {
        this.clearOldCache()
        // Пробуем еще раз
        localStorage.setItem(
          this.prefix + key,
          JSON.stringify({ data, timestamp: Date.now() })
        )
      }
    }
  }
  
  remove(key: string) {
    localStorage.removeItem(this.prefix + key)
  }
  
  clear() {
    const keys = Object.keys(localStorage)
    keys.forEach(key => {
      if (key.startsWith(this.prefix)) {
        localStorage.removeItem(key)
      }
    })
  }
  
  private clearOldCache() {
    // Удаляем старую версию кэша (v1_)
    const keys = Object.keys(localStorage)
    keys.forEach(key => {
      if (key.startsWith('v1_') || key.startsWith('cache_')) {
        localStorage.removeItem(key)
      }
    })
  }
}

export const localCache = new LocalStorageCache()
```

### Уровень 3: React Query Cache

```typescript
// utils/cache.tsx

import { QueryClient, QueryClientProvider } from '@tanstack/react-query'

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000, // 5 минут
      cacheTime: 10 * 60 * 1000, // 10 минут
      refetchOnWindowFocus: true,
      refetchOnReconnect: true,
      retry: 3
    }
  }
})

export function CacheProvider({ children }) {
  return (
    <QueryClientProvider client={queryClient}>
      {children}
    </QueryClientProvider>
  )
}

// Пример хука с кэшированием
export function useProducts(companyId?: number) {
  return useQuery({
    queryKey: ['products', companyId],
    queryFn: async () => {
      // 1. Проверяем RAM кэш
      const ramData = ramCache.get(`products_${companyId || 'all'}`)
      if (ramData) return ramData
      
      // 2. Проверяем localStorage
      const localData = localCache.get(`products_${companyId || 'all'}`)
      if (localData) {
        ramCache.set(`products_${companyId || 'all'}`, localData)
        return localData
      }
      
      // 3. Загружаем с API
      const response = await fetch(
        `/products${companyId ? `?company_id=${companyId}` : ''}`
      )
      const data = await response.json()
      
      // 4. Сохраняем во все кэши
      ramCache.set(`products_${companyId || 'all'}`, data.products)
      localCache.set(`products_${companyId || 'all'}`, data.products)
      
      return data.products
    }
  })
}
```

### Логика работы кэша

```typescript
// Порядок проверки при запросе данных:

async function getData(key: string) {
  // 1. Проверяем RAM (самый быстрый - 0ms)
  const ramData = ramCache.get(key)
  if (ramData) {
    console.log('✅ Данные из RAM')
    return ramData
  }
  
  // 2. Проверяем localStorage (~10ms)
  const localData = localCache.get(key)
  if (localData) {
    console.log('✅ Данные из localStorage')
    // Восстанавливаем в RAM
    ramCache.set(key, localData)
    return localData
  }
  
  // 3. Проверяем React Query (~50ms если в памяти)
  const cachedQuery = queryClient.getQueryData(['key'])
  if (cachedQuery) {
    console.log('✅ Данные из React Query')
    // Восстанавливаем в RAM и localStorage
    ramCache.set(key, cachedQuery)
    localCache.set(key, cachedQuery)
    return cachedQuery
  }
  
  // 4. Загружаем с API (~200-500ms)
  console.log('🌐 Загрузка с API')
  const response = await fetch('/api/data')
  const data = await response.json()
  
  // 5. Сохраняем во ВСЕ кэши
  ramCache.set(key, data)
  localCache.set(key, data)
  queryClient.setQueryData(['key'], data)
  
  return data
}
```

---

## 🔄 REALTIME СИСТЕМА

### Realtime Manager

```typescript
// utils/realtimeCache.tsx

class RealtimeManager {
  private subscriptions = new Map<string, any>()
  private reconnectTimers = new Map<string, NodeJS.Timeout>()
  private maxReconnectAttempts = 5
  private reconnectDelay = 3000
  
  /**
   * Подписка на изменения товаров
   */
  subscribeToProducts(companyId?: number) {
    const channelName = companyId ? `products_${companyId}` : 'products_all'
    
    if (this.subscriptions.has(channelName)) {
      console.log(`✅ Уже подписаны на ${channelName}`)
      return
    }
    
    console.log(`🔌 Подписка на ${channelName}...`)
    
    const channel = supabase
      .channel(channelName)
      .on(
        'postgres_changes',
        {
          event: '*', // INSERT, UPDATE, DELETE
          schema: 'public',
          table: 'products',
          ...(companyId ? { filter: `company_id=eq.${companyId}` } : {})
        },
        (payload) => {
          console.log(`🔔 Изменение в products:`, payload)
          
          // Очищаем ВСЕ кэши
          ramCache.delete(`products_${companyId || 'all'}`)
          localCache.remove(`products_${companyId || 'all'}`)
          queryClient.invalidateQueries({ queryKey: ['products'] })
          
          console.log(`✅ Кэши обновлены`)
        }
      )
      .subscribe((status, error) => {
        console.log(`📡 Статус ${channelName}: ${status}`)
        
        // Обработка ошибок с автоматическим переподключением
        if (status === 'CHANNEL_ERROR' || status === 'TIMED_OUT' || status === 'CLOSED') {
          console.error(`❌ Ошибка канала ${channelName}:`, error)
          this.reconnect(channelName, () => this.subscribeToProducts(companyId))
        } else if (status === 'SUBSCRIBED') {
          console.log(`✅ Успешно подписаны на ${channelName}`)
          // Очищаем таймеры переподключения
          const timer = this.reconnectTimers.get(channelName)
          if (timer) {
            clearTimeout(timer)
            this.reconnectTimers.delete(channelName)
          }
        }
      })
    
    this.subscriptions.set(channelName, channel)
  }
  
  /**
   * Автоматическое переподключение
   */
  private reconnect(channelName: string, subscribeFunction: () => void, attempt: number = 1) {
    if (attempt > this.maxReconnectAttempts) {
      console.error(`❌ Превышено максимальное количество попыток для ${channelName}`)
      return
    }
    
    const delay = this.reconnectDelay * attempt
    console.log(`🔄 Переподключение к ${channelName} через ${delay}ms (попытка ${attempt}/${this.maxReconnectAttempts})`)
    
    const timer = setTimeout(() => {
      this.unsubscribe(channelName)
      subscribeFunction()
    }, delay)
    
    this.reconnectTimers.set(channelName, timer)
  }
  
  /**
   * Переподключить все каналы
   */
  reconnectAll() {
    console.log('🔄 Переподключение всех каналов...')
    const channelNames = Array.from(this.subscriptions.keys())
    
    this.unsubscribeAll()
    
    channelNames.forEach(channelName => {
      if (channelName.startsWith('products_')) {
        const companyId = channelName === 'products_all' ? undefined : parseInt(channelName.replace('products_', ''))
        this.subscribeToProducts(companyId)
      } else if (channelName === 'companies') {
        this.subscribeToCompanies()
      } else if (channelName === 'ads') {
        this.subscribeToAds()
      }
    })
  }
  
  unsubscribe(channelName: string) {
    const subscription = this.subscriptions.get(channelName)
    if (subscription) {
      supabase.removeChannel(subscription)
      this.subscriptions.delete(channelName)
    }
  }
  
  unsubscribeAll() {
    for (const [channelName, subscription] of this.subscriptions.entries()) {
      supabase.removeChannel(subscription)
    }
    this.subscriptions.clear()
  }
}

export const realtimeManager = new RealtimeManager()

// Автоматическая инициализация
if (typeof window !== 'undefined') {
  realtimeManager.subscribeToProducts()
  realtimeManager.subscribeToCompanies()
  realtimeManager.subscribeToAds()
  
  // Page Visibility API - переподключение при возвращении на вкладку
  document.addEventListener('visibilitychange', () => {
    if (document.visibilityState === 'visible') {
      console.log('👁️ Пользователь вернулся - переподключаемся...')
      setTimeout(() => {
        realtimeManager.reconnectAll()
        ramCache.clear()
        queryClient.invalidateQueries()
      }, 1000)
    }
  })
  
  // Network Events - переподключение при восстановлении сети
  window.addEventListener('online', () => {
    console.log('🌐 Сеть восстановлена - переподключаемся...')
    realtimeManager.reconnectAll()
    ramCache.clear()
    queryClient.invalidateQueries()
  })
}
```

---

## 🌍 ЛОКАЛИЗАЦИЯ (Русский + Узбекский)

```typescript
// utils/translations.tsx

export type Language = 'ru' | 'uz'

export interface Translations {
  // Общие
  welcome: string
  save: string
  cancel: string
  delete: string
  // ... еще 500+ строк
}

export const translations: Record<Language, Translations> = {
  ru: {
    welcome: 'Добро пожаловать',
    save: 'Сохранить',
    cancel: 'Отмена',
    delete: 'Удалить',
    // ...
  },
  uz: {
    welcome: "Xush kelibsiz",
    save: "Saqlash",
    cancel: "Bekor qilish",
    delete: "O'chirish",
    // ...
  }
}

// Использование
import { useTranslation } from './utils/translations'

function Component() {
  const t = useTranslation()
  
  return <button>{t.save}</button>
}
```

---

## 🎨 ДИЗАЙН СИСТЕМА

### Темы (Светлая/Темная)

```typescript
// utils/ThemeContext.tsx

type Theme = 'light' | 'dark'

export const ThemeProvider = ({ children }) => {
  const [theme, setTheme] = useState<Theme>(() => {
    return localStorage.getItem('theme') as Theme || 'light'
  })

  const toggleTheme = () => {
    const newTheme = theme === 'light' ? 'dark' : 'light'
    setTheme(newTheme)
    localStorage.setItem('theme', newTheme)
    document.documentElement.classList.toggle('dark', newTheme === 'dark')
  }

  useEffect(() => {
    document.documentElement.classList.toggle('dark', theme === 'dark')
  }, [])

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  )
}
```

### Цветовая палитра (globals.css)

```css
:root {
  --background: #ffffff;
  --foreground: oklch(0.145 0 0);
  --primary: #030213;
  --secondary: oklch(0.95 0.0058 264.53);
  --accent: #e9ebef;
  --destructive: #d4183d;
  --border: rgba(0, 0, 0, 0.1);
}

.dark {
  --background: oklch(0.145 0 0);
  --foreground: oklch(0.985 0 0);
  --primary: oklch(0.985 0 0);
  --secondary: oklch(0.269 0 0);
  --accent: oklch(0.269 0 0);
  --destructive: oklch(0.396 0.141 25.723);
  --border: oklch(0.269 0 0);
}
```

### Анимации

```css
/* Радужный фон для карточек */
@keyframes rainbowBackground {
  0% {
    background: linear-gradient(135deg, rgba(255, 0, 0, 0.1) 0%, rgba(255, 255, 255, 0.95) 100%);
  }
  14.28% {
    background: linear-gradient(135deg, rgba(255, 127, 0, 0.1) 0%, rgba(255, 255, 255, 0.95) 100%);
  }
  /* ... остальные цвета ... */
  100% {
    background: linear-gradient(135deg, rgba(255, 0, 0, 0.1) 0%, rgba(255, 255, 255, 0.95) 100%);
  }
}

.rainbow-card-animation-light {
  animation: rainbowBackground 8s ease-in-out infinite;
}

/* Плавающие капли */
@keyframes floatingBlob {
  0% {
    transform: translate(0, 0) scale(1);
    border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
  }
  25% {
    transform: translate(30px, 40px) scale(1.1);
    border-radius: 30% 60% 70% 40% / 50% 60% 30% 60%;
  }
  /* ... */
  100% {
    transform: translate(0, 0) scale(1);
    border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
  }
}
```

---

## 💳 3-УРОВНЕВАЯ СИСТЕМА ОПЛАТЫ

```
УРОВЕНЬ 1: Покупатель → Компания
┌─────────────────────────────────┐
│ Покупатель оплачивает заказ 100%│
│ через Payme/Click               │
│                                 │
│ Например: 2500₸                 │
└─────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ Компания получает 100%          │
│                                 │
│ 2500₸ → на счет компании        │
└─────────────────────────────────┘

УРОВЕНЬ 2: Компания → Админ
┌─────────────────────────────────┐
│ Компания платит комиссию админу │
│ (5-10% от продаж)               │
│                                 │
│ Например: 250₸ (10% от 2500₸)   │
└─────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ Админ получает комиссию         │
│                                 │
│ 250₸ → на счет админа           │
└─────────────────────────────────┘

УРОВЕНЬ 3: Админ → Разработчик
┌─────────────────────────────────┐
│ Админ платит фиксированную сумму│
│ за использование системы        │
│                                 │
│ Например: $500/месяц            │
└─────────────────────────────────┘
```

---

## 📱 МОБИЛЬНАЯ ОПТИМИЗАЦИЯ

```typescript
// components/MobileOptimization.tsx

export function MobileOptimization() {
  useEffect(() => {
    // Отключить зум
    const viewport = document.querySelector('meta[name="viewport"]')
    if (viewport) {
      viewport.setAttribute(
        'content',
        'width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no'
      )
    }
    
    // Отключить Pull-to-Refresh
    document.body.style.overscrollBehaviorY = 'contain'
    
    // Touch-friendly размеры
    document.documentElement.style.setProperty('--touch-target-min', '44px')
  }, [])
  
  return null
}
```

### Адаптивность

```css
/* Tailwind breakpoints */
/* sm: 640px - мобильные (горизонтальная ориентация) */
/* md: 768px - планшеты */
/* lg: 1024px - десктопы */
/* xl: 1280px - большие десктопы */

/* Пример адаптивной сетки */
<div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
  {/* Карточки товаров */}
</div>
```

---

## 🔐 БЕЗОПАСНОСТЬ

### Хеширование паролей

```typescript
import bcrypt from 'bcryptjs'

// При регистрации
const passwordHash = await bcrypt.hash(password, 10)

// При входе
const passwordMatch = await bcrypt.compare(inputPassword, company.password_hash)
```

### Защита на сервере

```typescript
// Server: /supabase/functions/server/index.tsx

import { Hono } from 'npm:hono'
import { cors } from 'npm:hono/cors'
import { logger } from 'npm:hono/logger'

const app = new Hono()

// CORS
app.use('*', cors({
  origin: '*',
  credentials: true
}))

// Логирование
app.use('*', logger(console.log))

// Проверка авторизации для защищенных роутов
const requireAuth = async (c, next) => {
  const token = c.req.header('Authorization')?.split(' ')[1]
  
  if (!token) {
    return c.json({ error: 'Unauthorized' }, 401)
  }
  
  // Проверка токена...
  
  await next()
}

// Роуты с защитой
app.put('/make-server-6a74b22c/products/:id', requireAuth, async (c) => {
  // Обновление товара
})
```

---

## 🗄️ СТРУКТУРА БАЗЫ ДАННЫХ

### Таблицы PostgreSQL

```sql
-- kv_store_6a74b22c (key-value хранилище)
CREATE TABLE kv_store_6a74b22c (
  key TEXT PRIMARY KEY,
  value JSONB NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

-- products (товары)
CREATE TABLE products (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  quantity INTEGER NOT NULL DEFAULT 0,
  price NUMERIC(10, 2) NOT NULL,
  markup_percent NUMERIC(5, 2) NOT NULL DEFAULT 0,
  barcode TEXT,
  barid TEXT,
  category TEXT NOT NULL,
  company_id INTEGER NOT NULL, -- ВАЖНО! Всегда должен быть указан
  images TEXT[], -- Массив URL изображений
  colors TEXT[], -- Массив цветов
  available_to_customers BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW()
);

-- companies (компании)
CREATE TABLE companies (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  phone TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  access_key TEXT NOT NULL, -- 30 символов
  is_private BOOLEAN DEFAULT false,
  company_id TEXT UNIQUE, -- Для приватных компаний
  logo_url TEXT,
  address TEXT,
  description TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- users (пользователи-покупатели)
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  phone TEXT UNIQUE NOT NULL,
  registration_mode TEXT NOT NULL, -- 'public' или 'private'
  private_company_id TEXT, -- Если приватная регистрация
  created_at TIMESTAMP DEFAULT NOW()
);

-- customer_orders (заказы)
CREATE TABLE customer_orders (
  id SERIAL PRIMARY KEY,
  receipt_code TEXT UNIQUE NOT NULL,
  customer_phone TEXT NOT NULL,
  company_id INTEGER NOT NULL,
  items JSONB NOT NULL, -- [{product_id, quantity, price, final_price}]
  total_amount NUMERIC(10, 2) NOT NULL,
  status TEXT NOT NULL, -- 'pending', 'paid', 'cancelled'
  payment_method TEXT, -- 'payme', 'click', 'cash'
  created_at TIMESTAMP DEFAULT NOW()
);

-- ads (реклама)
CREATE TABLE ads (
  id SERIAL PRIMARY KEY,
  company_id INTEGER NOT NULL,
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  image_url TEXT NOT NULL,
  target_url TEXT,
  status TEXT NOT NULL, -- 'pending', 'approved', 'rejected', 'expired'
  rejection_reason TEXT,
  views_count INTEGER DEFAULT 0,
  starts_at TIMESTAMP,
  expires_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);

-- payments (платежи)
CREATE TABLE payments (
  id SERIAL PRIMARY KEY,
  company_id INTEGER NOT NULL,
  amount NUMERIC(10, 2) NOT NULL,
  payment_type TEXT NOT NULL, -- 'sales', 'commission', 'system_fee'
  status TEXT NOT NULL, -- 'pending', 'completed', 'failed'
  created_at TIMESTAMP DEFAULT NOW()
);

-- expenses (расходы)
CREATE TABLE expenses (
  id SERIAL PRIMARY KEY,
  company_id INTEGER NOT NULL,
  category TEXT NOT NULL,
  amount NUMERIC(10, 2) NOT NULL,
  description TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🚀 ВАЖНЫЕ ДЕТАЛИ РЕАЛИЗАЦИИ

### 1. company_id ВСЕГДА должен быть указан

```typescript
// ❌ НЕПРАВИЛЬНО
const product = {
  name: 'Товар',
  price: 100
}

// ✅ ПРАВИЛЬНО
const product = {
  name: 'Товар',
  price: 100,
  company_id: company_id // ОБЯЗАТЕЛЬНО!
}
```

### 2. Расчет итоговой цены

```typescript
const finalPrice = basePrice * (1 + markup_percent / 100)

// Пример:
// base_price = 800₸
// markup_percent = 25
// final_price = 800 * (1 + 25/100) = 800 * 1.25 = 1000₸
```

### 3. Автосписание товаров при оплате

```typescript
// После успешной оплаты:
for (const item of order.items) {
  await updateProduct(item.product_id, {
    quantity: currentProduct.quantity - item.quantity,
    company_id: company_id // НЕ ЗАБЫТЬ!
  })
}
```

### 4. Система маркеров категорий

```typescript
// Маркер пустой категории
const marker = {
  name: `__CATEGORY_MARKER__${categoryName}`,
  quantity: 0,
  price: 0,
  available_to_customers: false,
  company_id: company_id
}

// При отображении - фильтровать маркеры
const displayProducts = products.filter(p => 
  !p.name || !p.name.startsWith('__CATEGORY_MARKER__')
)
```

### 5. Обработка потери соединения

```typescript
// Page Visibility API
document.addEventListener('visibilitychange', () => {
  if (document.visibilityState === 'visible') {
    // Переподключить Realtime
    realtimeManager.reconnectAll()
    // Очистить кэши
    ramCache.clear()
    queryClient.invalidateQueries()
  }
})

// Network Events
window.addEventListener('online', () => {
  realtimeManager.reconnectAll()
  ramCache.clear()
  queryClient.invalidateQueries()
})
```

---

## 📝 ФИНАЛЬНЫЕ ИНСТРУКЦИИ ДЛЯ AI

1. **Начни с создания структуры проекта:**
   - Создай все файлы из раздела "Структура файлов"
   - Настрой Tailwind CSS v4
   - Подключи все необходимые библиотеки

2. **Реализуй систему пользователей:**
   - Создай все страницы для каждого типа пользователя
   - Реализуй логику аутентификации
   - Добавь систему навигации

3. **Реализуй компоненты:**
   - Создай все UI компоненты из shadcn/ui
   - Реализуй карточки товаров с анимациями
   - Добавь формы с валидацией

4. **Реализуй кэширование:**
   - Создай 3-уровневую систему кэша
   - Настрой React Query
   - Добавь Realtime подписки

5. **Настрой Supabase:**
   - Создай таблицы в базе данных
   - Реализуй Edge Functions
   - Настрой Storage для изображений

6. **Добавь платежную систему:**
   - Интеграцию с Payme
   - Интеграцию с Click
   - Логику автосписания товаров

7. **Реализуй аналитику:**
   - Графики с recharts
   - Фильтры по датам
   - Метрики и инсайты

8. **Добавь финальные детали:**
   - Локализация (RU/UZ)
   - Темная/светлая тема
   - Мобильная оптимизация
   - Анимации и переходы

**ВАЖНО:** Следуй ТОЧНО этому промпту! Каждая деталь важна для правильной работы системы!

---

**Конец промпта. Начинай создание проекта!** 🚀
