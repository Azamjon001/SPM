# 🔒 Система регистрации покупателей: Публичный и Приватный режимы

## Обзор системы

Реализована полная система двухрежимной регистрации покупателей с интуитивно понятным интерфейсом выбора.

## 🎯 Функциональность

### 1. **Экран выбора режима регистрации** (`CustomerRegistrationModeSelector`)
При первом входе в приложение покупатель видит красивый экран выбора между двумя режимами:

#### 🔵 Публичная регистрация (по умолчанию)
- Доступ ко всем публичным компаниям на платформе
- Широкий выбор товаров
- Без ограничений
- Стандартная регистрация с 3 полями:
  - Имя
  - Фамилия
  - Номер телефона

#### 🟣 Приватная регистрация
- Доступ только к одной конкретной компании по её ID
- Требуется знать ID компании
- Эксклюзивный доступ
- Расширенная регистрация с 4 полями:
  - Имя
  - Фамилия
  - Номер телефона
  - **ID компании** (новое поле с иконкой замка)

### 2. **Адаптивная форма регистрации** (`LoginPage`)
Форма регистрации автоматически адаптируется в зависимости от выбранного режима:

- **Публичный режим**: синяя цветовая схема, стандартные 3 поля
- **Приватный режим**: фиолетовая цветовая схема, 4 поля + подсказка об ID
- Иконка меняется (пользователь → замок)
- Заголовок изменяется на "Приватная регистрация"

### 3. **Валидация и проверка**
При приватной регистрации система:
1. Проверяет наличие ID компании
2. Запрашивает компанию из базы данных
3. Проверяет, что компания действительно приватная
4. Сохраняет связь покупателя с компанией в базу данных

## 🛠️ Технические детали

### Компоненты
- **`CustomerRegistrationModeSelector.tsx`** - Экран выбора режима (новый)
- **`LoginPage.tsx`** - Обновленная форма с поддержкой приватного режима
- **`App.tsx`** - Интеграция и логика переключения

### Состояния в App.tsx
```typescript
const [customerRegistrationMode, setCustomerRegistrationMode] = useState<'public' | 'private' | null>(null);
const [privateCompanyId, setPrivateCompanyId] = useState<string | null>(null);
```

### Навигация
```
customerModeSelector → login (public/private) → home
                    ↓
              companyLogin
```

### API интеграция
При регистрации в приватном режиме:
```typescript
await addUser({ 
  first_name: userData.firstName,
  last_name: userData.lastName,
  phone_number: userData.phone,
  company_id: userData.companyId || null  // 🔒 Новое поле
});
```

## 🎨 UI/UX особенности

### Экран выбора режима
- Градиентный фон (blue-purple-pink)
- Две интерактивные карточки с hover эффектами
- Иконки: Store (публичный) и Lock (приватный)
- Детальное описание каждого режима
- Кнопка "Вход для компании" внизу

### Форма приватной регистрации
- Фиолетовая акцентная цветовая схема
- Специальное поле с иконкой замка
- Подсказка: "💡 ID компании можно получить у менеджера компании"
- Кнопка "Назад" для возврата к выбору режима

## 📱 Пользовательский сценарий

### Публичная регистрация
1. Пользователь открывает приложение
2. Видит экран выбора режима
3. Выбирает "Публичная"
4. Заполняет 3 поля (имя, фамилия, телефон)
5. Нажимает "Войти"
6. Получает доступ ко всем публичным компаниям

### Приватная регистрация
1. Пользователь открывает приложение
2. Видит экран выбора режима
3. Выбирает "Приватная"
4. Заполняет 4 поля (имя, фамилия, телефон, ID компании)
5. Система проверяет ID компании
6. При успешной проверке создает аккаунт
7. Пользователь получает доступ только к этой компании

## 🔐 Безопасность

- ID компании проверяется на сервере перед регистрацией
- Проверяется флаг `is_private` компании
- Связь покупателя с компанией сохраняется в БД
- Невозможно зарегистрироваться с несуществующим ID
- Невозможно использовать ID публичной компании в приватном режиме

## ✅ Статус реализации

- [x] Создан компонент `CustomerRegistrationModeSelector`
- [x] Обновлен `LoginPage` с поддержкой приватного режима
- [x] Интегрировано в `App.tsx`
- [x] Добавлена валидация ID компании
- [x] Реализована проверка на сервере
- [x] Добавлена подсказка для пользователей
- [x] Настроена навигация (кнопка "Назад")
- [x] При выходе пользователь возвращается к выбору режима

## 🎯 Следующие шаги

Для полной реализации приватных компаний необходимо:
1. Фильтровать товары в `HomePage` по `company_id` для приватных покупателей
2. Добавить индикатор приватного режима в интерфейс
3. Реализовать панель управления для приватных компаний

---

**Дата создания**: 10 января 2026
**Версия**: 1.0
**Статус**: ✅ Полностью реализовано и готово к использованию
