# 🔄 Исправление проблемы разрыва соединения при долгом бездействии (AFK)

**Дата:** 10.01.2026  
**Проблема:** При долгом бездействии (AFK) система выдает ошибку соединения и не загружает данные

## 🎯 Основные проблемы

### 1. **Отключено автообновление токенов**
В `/utils/supabase/client.tsx` было установлено `autoRefreshToken: false`, что приводило к истечению токенов при долгих сессиях.

### 2. **Отсутствие механизмов переподключения**
В системе Realtime отсутствовали:
- Обработка ошибок соединения
- Автоматическое переподключение при разрыве
- Мониторинг состояния соединения
- Восстановление соединения при возвращении на вкладку

### 3. **Отсутствие обработки сетевых событий**
Не обрабатывались события:
- Возвращение пользователя на вкладку (Page Visibility API)
- Потеря/восстановление сетевого соединения

## ✅ Решения

### 1. **Supabase Client (`/utils/supabase/client.tsx`)**

#### Включено автообновление токенов:
```typescript
auth: {
  persistSession: false,
  autoRefreshToken: true // ✅ ИСПРАВЛЕНО
}
```

#### Добавлены настройки Realtime:
```typescript
realtime: {
  params: {
    eventsPerSecond: 10
  },
  timeout: 10000, // 10 секунд для первого подключения
  heartbeatIntervalMs: 30000, // Проверка соединения каждые 30 секунд
}
```

### 2. **RealtimeManager (`/utils/realtimeCache.tsx`)**

#### Добавлен механизм автоматического переподключения:
```typescript
private reconnect(channelName: string, subscribeFunction: () => void, attempt: number = 1) {
  if (attempt > this.maxReconnectAttempts) {
    console.error(`❌ Превышено максимальное количество попыток`);
    return;
  }
  
  const delay = this.reconnectDelay * attempt; // Экспоненциальная задержка
  setTimeout(() => {
    this.unsubscribe(channelName);
    subscribeFunction();
  }, delay);
}
```

#### Обработка статусов соединения:
- `CHANNEL_ERROR` → Автоматическое переподключение
- `TIMED_OUT` → Автоматическое переподключение
- `CLOSED` → Автоматическое переподключение
- `SUBSCRIBED` → Очистка таймеров переподключения

#### Метод переподключения всех каналов:
```typescript
reconnectAll() {
  const channelNames = Array.from(this.subscriptions.keys());
  this.unsubscribeAll();
  
  // Заново подписываемся на все каналы
  channelNames.forEach(channelName => {
    // Переподключение к products, companies, ads, customer_orders
  });
}
```

### 3. **Page Visibility API**

#### Автоматическое переподключение при возвращении на вкладку:
```typescript
document.addEventListener('visibilitychange', () => {
  if (document.visibilityState === 'visible') {
    console.log('👁️ Пользователь вернулся на вкладку');
    
    setTimeout(() => {
      realtimeManager.reconnectAll();
      ramCache.clear();
      queryClient.invalidateQueries();
    }, 1000);
  }
});
```

### 4. **Network Events API**

#### Обработка восстановления сети:
```typescript
window.addEventListener('online', () => {
  console.log('🌐 Сеть восстановлена - переподключаемся...');
  realtimeManager.reconnectAll();
  ramCache.clear();
  queryClient.invalidateQueries();
});

window.addEventListener('offline', () => {
  console.warn('❌ Потеряно сетевое соединение');
});
```

## 📊 Параметры переподключения

| Параметр | Значение | Описание |
|----------|----------|----------|
| `maxReconnectAttempts` | 5 | Максимальное количество попыток переподключения |
| `reconnectDelay` | 3000ms | Базовая задержка между попытками |
| `heartbeatIntervalMs` | 30000ms | Проверка соединения каждые 30 секунд |
| `timeout` | 10000ms | Таймаут для первого подключения |

## 🎯 Сценарии работы

### Сценарий 1: Пользователь долго AFK
1. Realtime соединение разрывается из-за бездействия
2. Статус меняется на `CLOSED` или `TIMED_OUT`
3. Автоматически запускается переподключение с экспоненциальной задержкой
4. Максимум 5 попыток переподключения (3s, 6s, 9s, 12s, 15s)

### Сценарий 2: Пользователь переключил вкладку
1. Пользователь переключается на другую вкладку
2. Браузер переводит вкладку в неактивное состояние
3. При возвращении срабатывает `visibilitychange`
4. Автоматически переподключаются все каналы
5. Очищаются все кэши и загружаются свежие данные

### Сценарий 3: Потеря интернет-соединения
1. Пользователь теряет интернет (WiFi отключился)
2. Срабатывает событие `offline`
3. При восстановлении срабатывает `online`
4. Автоматически переподключаются все каналы
5. Загружаются актуальные данные

## 📋 Изменённые файлы

1. `/utils/supabase/client.tsx`
   - Включено `autoRefreshToken: true`
   - Добавлены настройки Realtime с heartbeat

2. `/utils/realtimeCache.tsx`
   - Добавлен метод `reconnect()` для автоматического переподключения
   - Добавлена обработка статусов соединения в `subscribeToProducts()`
   - Добавлен метод `reconnectAll()` для массового переподключения
   - Добавлена обработка Page Visibility API
   - Добавлена обработка Network Events API

## 🧪 Тестирование

### Тест 1: Долгое бездействие
1. Откройте приложение
2. Оставьте вкладку открытой на 30+ минут
3. Вернитесь и попробуйте работать с данными
4. **Ожидаемый результат:** Данные загружаются без ошибок

### Тест 2: Переключение вкладок
1. Откройте приложение
2. Переключитесь на другую вкладку на 10+ минут
3. Вернитесь на вкладку с приложением
4. **Ожидаемый результат:** Автоматическое переподключение и обновление данных

### Тест 3: Потеря интернета
1. Откройте приложение
2. Отключите WiFi/интернет
3. Подождите 30 секунд
4. Включите WiFi/интернет обратно
5. **Ожидаемый результат:** Автоматическое переподключение без ошибок

## 📝 Логирование

Все события соединения логируются в консоль:
- `🔌 [REALTIME] Подписка на...` - Начало подписки
- `📡 [REALTIME] Статус...` - Изменение статуса
- `❌ [REALTIME] Ошибка канала...` - Ошибка соединения
- `🔄 [REALTIME] Переподключение...` - Попытка переподключения
- `✅ [REALTIME] Успешно подписаны...` - Успешное подключение
- `👁️ [REALTIME] Пользователь вернулся...` - Возвращение на вкладку
- `🌐 [REALTIME] Сеть восстановлена...` - Восстановление интернета

## 🎉 Результат

✅ Приложение теперь работает стабильно при долгом бездействии  
✅ Автоматическое переподключение при разрыве соединения  
✅ Восстановление соединения при возвращении на вкладку  
✅ Обработка потери/восстановления интернета  
✅ Автообновление токенов для долгих сессий  
✅ Heartbeat для мониторинга состояния соединения  

## 💡 Рекомендации

1. **Мониторинг:** Следите за логами в консоли для отладки проблем соединения
2. **Таймауты:** При необходимости можно увеличить `heartbeatIntervalMs` до 60000ms (1 минута)
3. **Попытки переподключения:** Можно увеличить `maxReconnectAttempts` для более настойчивого переподключения
4. **Кэширование:** RAM кэш автоматически очищается каждые 2 минуты для освобождения памяти
