# 🐛 Исправление кнопки удаления товара (10.01.2026)

## ❌ Проблема
Кнопка удаления товара в компоненте редактирования не работала - при нажатии товар не удалялся.

## 🔍 Причина
В функции `handleDelete` в файле `/components/DigitalWarehouse.tsx` неправильно вызывалась функция API:

```typescript
// ❌ НЕПРАВИЛЬНО - передается 2 параметра
await deleteProduct(companyId, id);
```

Но функция `deleteProduct` в `/utils/api.tsx` принимает только один параметр:

```typescript
export async function deleteProduct(id: number) {
  await apiCall(`/products/${id}`, {
    method: 'DELETE',
  });
}
```

## ✅ Решение

Исправлен вызов функции в `/components/DigitalWarehouse.tsx` (строка 190):

```typescript
// ✅ ПРАВИЛЬНО - передается только id
await deleteProduct(Number(id));
```

## 📋 Изменённые файлы
- `/components/DigitalWarehouse.tsx` - исправлен вызов `deleteProduct()`

## 🎯 Результат
- ✅ Кнопка удаления товара теперь работает корректно
- ✅ Товар удаляется из базы данных при нажатии на кнопку "Удалить"
- ✅ После удаления товара список обновляется автоматически
- ✅ Сохранена логика создания маркеров для пустых категорий

## 🧪 Тестирование
1. Откройте панель компании → Цифровой склад
2. Нажмите кнопку "Удалить" (🗑️) у любого товара
3. Подтвердите удаление
4. Товар должен исчезнуть из списка

## 💡 Комментарий
Эта ошибка возникла из-за несоответствия сигнатуры функции API и её вызова. 
В будущем рекомендуется использовать TypeScript strict mode для автоматического выявления таких проблем.
