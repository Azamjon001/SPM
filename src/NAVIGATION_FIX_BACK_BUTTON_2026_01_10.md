# 🧭 Исправление навигации с поддержкой кнопки "Назад" (10.01.2026)

## ❌ Проблема

При нажатии кнопки "Назад" на телефоне Android/iOS приложение **полностью закрывалось** вместо возврата на предыдущую страницу внутри приложения.

### Причина:
Все страницы управлялись через `useState` с условным рендерингом, что **не создавало истории браузера**:

```typescript
// ❌ НЕПРАВИЛЬНО - нет истории браузера
const [currentPage, setCurrentPage] = useState('home');
setCurrentPage('settings'); // Переход на другую страницу
```

При таком подходе:
- ✅ Страницы переключаются внутри приложения
- ❌ История браузера не создается
- ❌ Кнопка "Назад" закрывает приложение вместо возврата назад

## ✅ Решение

Создана **новая система навигации** с использованием **History API**, которая:
1. Создает правильную историю браузера
2. Обрабатывает кнопку "Назад" на телефоне
3. Поддерживает программный возврат назад
4. Сохраняет данные страницы

---

## 📁 Новый файл: `/utils/navigation.tsx`

### Основные компоненты:

#### 1. **NavigationManager** (Singleton)
Центральный менеджер навигации с поддержкой History API

```typescript
class NavigationManager {
  // Навигация на новую страницу (создает запись в истории)
  navigateTo(page: PageType, data?: any);
  
  // Программный возврат назад
  goBack();
  
  // Замена текущей страницы (не создает новую запись)
  replace(page: PageType, data?: any);
  
  // Сброс навигации к начальной странице
  reset(page?: PageType);
  
  // Подписка на изменения навигации
  subscribe(callback: (state: NavigationState) => void);
}
```

#### 2. **useNavigation** Hook
React хук для использования навигации в компонентах

```typescript
const { currentPage, navigateTo, goBack, replace, canGoBack } = useNavigation();
```

---

## 🔧 Изменения в `/App.tsx`

### До (❌):
```typescript
const [currentPage, setCurrentPage] = useState('home');

// Переход на другую страницу
setCurrentPage('settings');

// Возврат назад
setCurrentPage('home');
```

### После (✅):
```typescript
const { currentPage, navigateTo, goBack } = useNavigation();

// Переход на другую страницу (создает запись в истории)
navigateTo('settings');

// Возврат назад (использует History API)
goBack();
```

---

## 🎯 Как это работает

### 1. **Переход на новую страницу**
```typescript
navigateTo('settings');
```
- Создает новую запись в `window.history`
- Обновляет URL hash: `#settings`
- Уведомляет все компоненты об изменении
- **Кнопка "Назад" теперь работает!**

### 2. **Обработка кнопки "Назад"**
```typescript
window.addEventListener('popstate', (event) => {
  // Автоматически обрабатывается NavigationManager
  // Компоненты получают обновление через subscribe()
});
```

### 3. **Предотвращение выхода из приложения**
```typescript
if (window.history.length <= 1) {
  event.preventDefault();
  navigationManager.reset('customerModeSelector');
}
```

---

## 📱 Поддерживаемые страницы

Все страницы теперь имеют правильную навигацию:

| Страница | Путь | Описание |
|----------|------|----------|
| `customerModeSelector` | `#customerModeSelector` | Выбор режима регистрации |
| `login` | `#login` | Страница входа |
| `sms` | `#sms` | SMS верификация |
| `home` | `#home` | Главная страница |
| `likes` | `#likes` | Избранное |
| `settings` | `#settings` | Настройки |
| `payment` | `#payment` | Оплата |
| `admin` | `#admin` | Админ панель |
| `company` | `#company` | Панель компании |
| `companyLogin` | `#companyLogin` | Вход компании |
| `companyKey` | `#companyKey` | Ключ доступа компании |

---

## 💡 Примеры использования

### В компонентах:

#### Переход на страницу настроек:
```typescript
<button onClick={() => navigateTo('settings')}>
  Настройки
</button>
```

#### Возврат назад:
```typescript
<button onClick={() => goBack()}>
  ← Назад
</button>
```

#### Переход с данными:
```typescript
navigateTo('payment', { 
  amount: 1000, 
  orderId: '12345' 
});

// В компоненте Payment:
const { pageData } = useNavigation();
console.log(pageData.amount); // 1000
console.log(pageData.orderId); // '12345'
```

#### Замена текущей страницы (без создания истории):
```typescript
replace('home'); // Не создает новую запись в истории
```

---

## 🧪 Тестирование

### Тест 1: Навигация вперед
1. Откройте приложение
2. Перейдите: Главная → Избранное → Настройки
3. **Ожидаемый результат:** URL меняется на `#home`, `#likes`, `#settings`

### Тест 2: Кнопка "Назад" на телефоне
1. Откройте приложение на телефоне
2. Перейдите: Главная → Настройки
3. Нажмите физическую кнопку "Назад" на телефоне
4. **Ожидаемый результат:** Возврат на Главную страницу (не выход из приложения)

### Тест 3: Кнопка "Назад" в браузере
1. Откройте приложение в браузере
2. Перейдите: Главная → Корзина → Оплата
3. Нажмите кнопку "Назад" в браузере
4. **Ожидаемый результат:** Корзина → Главная → Вход

### Тест 4: Предотвращение выхода
1. Откройте приложение
2. На первой странице нажмите кнопку "Назад"
3. **Ожидаемый результат:** Приложение НЕ закрывается, остаетесь на той же странице

---

## 📋 Изменённые файлы

### 1. `/utils/navigation.tsx` (НОВЫЙ)
- `NavigationManager` - менеджер навигации
- `useNavigation` - React хук
- Обработка `popstate` события
- Предотвращение выхода из приложения

### 2. `/App.tsx` (ИЗМЕНЁН)
- Импорт `useNavigation`
- Замена `useState` на `useNavigation()`
- Замена всех `setCurrentPage` на `navigateTo()`
- Использование `goBack()` вместо `setCurrentPage('prev')`

---

## 🔍 Логирование

Все события навигации логируются в консоль:

```
🧭 [Navigation] Инициализирован с поддержкой кнопки "Назад"
🧭 [Navigation] Переход на: settings
🔙 [Navigation] Нажата кнопка "Назад": { page: 'home' }
🔄 [Navigation] Сброс навигации к: customerModeSelector
⚠️ [Navigation] Попытка выхода из приложения предотвращена
```

---

## ⚙️ Технические детали

### History API
```typescript
// Добавление в историю
window.history.pushState(state, '', url);

// Замена текущей записи
window.history.replaceState(state, '', url);

// Программный возврат
window.history.back();

// Обработка события
window.addEventListener('popstate', handler);
```

### URL Hash
Используется для визуального отображения текущей страницы:
```
https://your-app.com/#home
https://your-app.com/#settings
https://your-app.com/#likes
```

### State Management
```typescript
interface NavigationState {
  page: PageType;
  data?: any; // Дополнительные данные
}
```

---

## 🎉 Результат

✅ **Кнопка "Назад"** на телефоне работает правильно  
✅ **История браузера** создается корректно  
✅ **Переходы между страницами** отслеживаются  
✅ **Предотвращен выход** из приложения при попытке вернуться с первой страницы  
✅ **Поддержка данных** при переходах между страницами  
✅ **Программный возврат** назад через `goBack()`  
✅ **Замена страницы** без создания истории через `replace()`  

---

## 🐛 Известные ограничения

1. **Обновление страницы:** При обновлении страницы (F5) состояние может сброситься. Для решения можно добавить сохранение в `localStorage`.

2. **Глубокие ссылки:** Прямые ссылки на конкретные страницы требуют дополнительной обработки при инициализации.

3. **SEO:** Hash-based routing не оптимален для SEO, но для PWA приложения это не критично.

---

## 💡 Рекомендации

### Для добавления новых страниц:

1. Добавьте тип страницы в `PageType`:
```typescript
export type PageType = 
  | 'home'
  | 'settings'
  | 'myNewPage'; // ← Добавить сюда
```

2. Используйте в компоненте:
```typescript
{currentPage === 'myNewPage' && (
  <MyNewPage onBack={() => goBack()} />
)}
```

3. Навигация на новую страницу:
```typescript
navigateTo('myNewPage', { someData: 123 });
```

---

## 🔮 Будущие улучшения

- [ ] Сохранение истории в `localStorage` для восстановления после перезагрузки
- [ ] Анимации переходов между страницами
- [ ] Gesture-based навигация (свайп влево/вправо)
- [ ] Breadcrumbs для визуализации пути
- [ ] Deep linking для прямых ссылок на страницы
